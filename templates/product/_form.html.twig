{#{{ form_start(form) }}#}
{#    {{ form_widget(form) }}#}
{#    <button class="btn">{{ button_label|default('Сохранить') }}</button>#}
{#{{ form_end(form) }}#}


{% block body %}
 <div class="container">
   {{ form_start(form) }}
   {{ form_widget(form) }}
   </div>
{{ form_end(form) }}
       
{% endblock %}